<!DOCTYPE html>
<html>
  <head>
    <title>CodeQuiz</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-[#111827] text-slate-300">
    <header class="bg-[#1F2937] shadow-lg py-4"> <%# py-4 para mais padding vertical %>
      <nav class="container mx-auto px-6 flex justify-between items-center"> <%# px-6 para mais padding horizontal %>
        <%= link_to 'Code Quiz', root_path, class: "text-2xl font-extrabold text-[#a5b4fc] hover:text-white transition-colors" %> <%# Cor do texto mais vibrante, hover, font mais pesada %>
        <div class="flex items-center space-x-6"> <%# Espaçamento maior entre os itens %>
          <% if user_signed_in? %>
            <span class="text-slate-400 text-sm">
              Logado como: <strong class="text-white"><%= current_user.email %></strong>
            </span>
            <%= button_to "Sair", destroy_user_session_path, method: :delete, class: "inline-block bg-red-600 hover:bg-red-700 text-white font-semibold py-2 px-4 rounded-full shadow-md transition-all" %> <%# rounded-full e shadow-md para um look mais premium %>
          <% else %>
            <%= link_to "Entrar", new_user_session_path, class: "text-slate-300 hover:text-[#a5b4fc] transition-colors" %>
            <%= link_to "Cadastrar", new_user_registration_path, class: "ml-4 bg-[#4f46e5] hover:bg-[#4338ca] text-white font-semibold py-2 px-4 rounded-full shadow-md transition-all" %> <%# Botão de cadastro no tema roxo %>
          <% end %>
        </div>
      </nav>
    </header>

    <main class="container mx-auto mt-12 px-5">
      <% if notice %>
        <p class="bg-green-900 border border-green-700 text-green-200 px-4 py-3 rounded-lg relative mb-4" role="alert"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="bg-red-900 border border-red-700 text-red-200 px-4 py-3 rounded-lg relative mb-4" role="alert"><%= alert %></p>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>